#!/bin/sh

set -e

if [ "$1" = "upgrade" ] || [ "$1" = "configure" ]; then
    update-alternatives --quiet --remove mips-linux-gnu-gcc /usr/bin/mips-linux-gnu-gcc-8
    update-alternatives --quiet --remove mips-linux-gnu-gcov /usr/bin/mips-linux-gnu-gcov-8
fi



exit 0
